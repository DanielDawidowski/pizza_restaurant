import React, { FC, ReactElement } from "react";
import Layout from "../../components/layout/Layout";

const Cancel: FC = (): ReactElement => {
  return <Layout>Sorry to see you cancelled your payment!</Layout>;
};

export default Cancel;
